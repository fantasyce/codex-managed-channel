[package]
name = "codex-managed-channel"
version = "0.2.0"
edition = "2024"
license = "MIT"

[dependencies]
anyhow = "1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml_edit = { version = "0.22", features = ["serde"] }

[dev-dependencies]
tempfile = "3"
